/// SwiftTUIiOS — re-exports SwiftTUI for iOS use.
///
/// You can import either `SwiftTUI` or `SwiftTUIiOS` — both work identically.
/// On iOS, only ST-prefixed type names are public, so there are no conflicts
/// with SwiftUI.

@_exported import SwiftTUI
